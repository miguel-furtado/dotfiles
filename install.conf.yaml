- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.shellcommon: shellcommon
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/.config/nvim: config/nvim
    ~/.config/starship.toml: config/starship.toml
    ~/.config/chameleon: config/chameleon
    ~/.config/tealdeer: config/tealdeer
    ~/.config/systemd/user/distrobox-upgrade.service: config/systemd/user/distrobox-upgrade.service
    ~/.config/systemd/user/distrobox-upgrade.timer: config/systemd/user/distrobox-upgrade.timer

